apiVersion: apps/v1
kind: Deployment
metadata:
  name: poi
  namespace: tripinsight
  labels:
    app.kubernetes.io/name: poi
spec:
  auth_enabled: FALSE
  replicas: 1
  selector:
    matchLabels:
      app: poi
  template:
    metadata:
      labels:
        app: poi
        version: beta-0514-1915
        deploy: 21-05-14-19-15-24
   
    spec:
      containers:
        - name: poi
          image: registryhhm0245.azurecr.io/poi:latest
          imagePullPolicy: Always        
          
          args: 
          - --SQL_USER
          - sqladminhHm0245
          - --SQL_PASSWORD 
          - uL6nj2Qo1 
          - --SQL_SERVER 
          - sqlserverhhm0245.database.windows.net


          ports:
            - name: http
              containerPort: 8083
              protocol: TCP

          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: poi
  namespace: tripinsight
spec:
  type: ClusterIP
  ports:
    - port: 8083
      targetPort: 8083
      protocol: TCP
      name: http
  selector:
    app: poi
 


---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: poi
  namespace: tripinsight
  annotations:
    kubernetes.io/ingress.class: addon-http-application-routing
spec:
  rules:
  - host: poi.aks-oh-dns-ddc81f8f.hcp.ukwest.azmk8s.io
    http:
      paths:
      - backend:
          serviceName: poi
          servicePort: 8083
        path: /api/poi/healthcheck

